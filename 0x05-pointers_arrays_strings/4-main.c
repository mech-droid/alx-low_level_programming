#include "main.h"

/**
 * main - check the code 
 *
 * Return: Always void.
 */

int main(void)
{
	char *str;

	str = "I do not fear computers. I fear the lack of them - Isaac asimov";
	print_rev(str);
	return (0);
}
