#include "main.h"

/**
 * int _isupper(int c) Writes a function that checks for uppercase character
 * @c: the character to be checked
 * Return: 1 if c is uppercase, 0 0therwise
 */

int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
